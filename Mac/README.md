My Mac Dotfiles Backup.

## Introduction

include `vimrc`, `zshrc`, `gitconfig`, `gitignore_global`, `powerlevel9k` ...

## Installation

```bash
$ git clone https://github.com/ehlxr/dotfiles.git ~/dotfiles
$ cd ~/dotfiles/Mac
$ chmod +x setup.sh
$ ./setup.sh
```
